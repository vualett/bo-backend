name: Deploy Application

on:
  push:
    branches: 
      - main

defaults:
  run:
    shell: bash

jobs:
  build-and-deploy:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Get short SHA
        id: git-sha
        uses: hudsonm62/short-sha@v1
        with:
          length: 7

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '14.21.3'

      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          repository: ualett/infra-iac-aws
          token: ${{ secrets.GH_PAT }}
        timeout-minutes: 5
        continue-on-error: false

      - uses: actions/setup-go@v5
        with:
          go-version: 1.18.1
          cache: false
        timeout-minutes: 5
        continue-on-error: false

      - run: |
          pwd && ls -ltr ../ && \
          ./bin/golang/ssm-params-downloads-by-env -org Ualett -env SandBox -scheduled no -app backend

      - name: Install dependencies
        run: npm install > /dev/null || { echo "An error occurred."; exit 1; }

      - name: Set up Meteor
        uses: meteorengineer/setup-meteor@v2
        with:
          meteor-release: '2.15'

      - name: Listing Files
        run: |
          ls -ltr && pwd && node -v && npm -v 

      - name: Set up Meteor
        uses: meteorengineer/setup-meteor@v2
        with:
          meteor-release: '2.15'

      # - name: Build application
      #   run: |
      #     pwd && ls -ltr
      #     # meteor build --server-only --architecture os.linux.x86_64 --directory bundle
      #     # cp .env ecosystem.config.js bundle/bundle